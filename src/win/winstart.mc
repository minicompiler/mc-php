// winstart.mc -- mc's entry point, `mc_start`, what `-entry:` names when the
// compiler is linked. A file of its own because it declares `main` extern and
// the compiler defines it.
#include <sys_windows_start>
