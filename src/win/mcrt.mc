// mcrt.mc -- mc's POSIX shims over kernel32, the object every Windows mc-php
// links next to itself (mc's docs/guide/95-windows-host.md § 3). One line: the
// file is mc's, resolved out of the library tree beside `mc`.
#include <sys_windows_host>
